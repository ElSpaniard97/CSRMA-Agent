# =========================
# AI Troubleshooting Agent - Environment Variables
# =========================

# REQUIRED: OpenAI API Key
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-key-here

# REQUIRED: JWT Secret for authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-long-random-secret-here-min-32-chars

# REQUIRED: Admin username
ADMIN_USERNAME=admin

# REQUIRED: Admin password (MUST BE BCRYPT HASH, NOT PLAIN TEXT!)
# Generate hash: node -e "console.log(require('bcryptjs').hashSync('YourPassword123', 10))"
# Example hash format: $2a$10$rZ8kQ...
ADMIN_PASSWORD_HASH=$2a$10$your-bcrypt-hash-here

# OPTIONAL: Server configuration
PORT=10000
NODE_ENV=production

# OPTIONAL: Storage paths (Render provides /data by default)
SETTINGS_PATH=/data/settings.json
SCRIPTS_DIR=/data/scripts

# =========================
# NOTES:
# =========================
# 
# 1. NEVER commit this file with real values to git
# 2. Copy to .env for local development
# 3. Set these in Render dashboard for production
# 4. Password MUST be bcrypt hash (use bcryptjs to generate)
# 5. JWT_SECRET should be at least 32 characters
#
# For Render deployment:
# - Go to Dashboard → Your Service → Environment
# - Add each variable individually
# - Click "Save Changes"
# - Service will auto-redeploy
